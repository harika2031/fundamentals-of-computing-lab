#include <stdio.h>

int main() {
    int n, r = 0;

    printf("Enter an integer number: ");
    scanf("%d", &n);

    int t = n;
    while (t != 0) {
        int d = t % 10;
        r = r * 10 + d;
        t /= 10;
    }

    printf("Reversed number: %d\n", r);

    return 0;
}
