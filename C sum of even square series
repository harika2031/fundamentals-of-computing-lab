#include <stdio.h>

int main() {
    int n, sum = 0;

    printf("Enter a number (n): ");
    scanf("%d", &n);

    for (int i = 2; i <= n; i += 2) {
        sum += i * i;
    }

    printf("Sum of the series: %d\n", sum);

    return 0;
}
