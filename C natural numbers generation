#include <stdio.h>

int main() {
    int n;

    printf("Enter a number (n): ");
    scanf("%d", &n);

    printf("Number series: ");
    for (int i = 1; i <= n; i++) {
        printf("%d ", i);
    }

    return 0;
}
